//this is kind of the origin file
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; //needs same name as below render
import './index.css';

ReactDOM.render(
  <App />,
  document.getElementById('root')
);
